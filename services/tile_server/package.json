{
  "name": "tile_server",
  "version": "0.0.1",
  "description": "tile server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "export NODE_ENV=development && nodemon server.js",
    "build": "export NODE_ENV=production && nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.3",
    "nodemon": "^2.0.2",
    "tiny-osmpbf": "^1.0.0-beta.1"
  }
}
